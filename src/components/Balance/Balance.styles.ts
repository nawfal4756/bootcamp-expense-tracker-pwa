import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
	balance: {
		fontWeight: 'bold',
		marginTop: '4%',
		fontSize: '50px',
		textAlign: 'center'
	},
	text: {
		textAlign: 'center'
	}
});
